    
    my $prefix;
    my $ran_num = int(rand(4975));
    
    if ($ran_num < 25) {$prefix = 'ос';}
    elsif ($ran_num < 27) {$prefix = 'ун';}
    elsif ($ran_num < 620) {$prefix = 'п';}
    elsif ($ran_num < 622) {$prefix = 'ог';}
    elsif ($ran_num < 667) {$prefix = 'из';}
    elsif ($ran_num < 675) {$prefix = 'уд';}
    elsif ($ran_num < 684) {$prefix = 'ул';}
    elsif ($ran_num < 748) {$prefix = 'от';}
    elsif ($ran_num < 926) {$prefix = 'ч';}
    elsif ($ran_num < 931) {$prefix = 'ан';}
    elsif ($ran_num < 987) {$prefix = 'об';}
    elsif ($ran_num < 1244) {$prefix = 'б';}
    elsif ($ran_num < 1246) {$prefix = 'ам';}
    elsif ($ran_num < 1256) {$prefix = 'их';}
    elsif ($ran_num < 1260) {$prefix = 'ум';}
    elsif ($ran_num < 1263) {$prefix = 'ук';}
    elsif ($ran_num < 1283) {$prefix = 'ил';}
    elsif ($ran_num < 1284) {$prefix = 'ов';}
    elsif ($ran_num < 1285) {$prefix = 'й';}
    elsif ($ran_num < 1425) {$prefix = 'р';}
    elsif ($ran_num < 1771) {$prefix = 'к';}
    elsif ($ran_num < 2121) {$prefix = 'т';}
    elsif ($ran_num < 2122) {$prefix = 'ат';}
    elsif ($ran_num < 2124) {$prefix = 'ей';}
    elsif ($ran_num < 2140) {$prefix = 'ц';}
    elsif ($ran_num < 2143) {$prefix = 'уч';}
    elsif ($ran_num < 2150) {$prefix = 'яв';}
    elsif ($ran_num < 2151) {$prefix = 'ех';}
    elsif ($ran_num < 2156) {$prefix = 'эп';}
    elsif ($ran_num < 2175) {$prefix = 'ф';}
    elsif ($ran_num < 2177) {$prefix = 'ед';}
    elsif ($ran_num < 2267) {$prefix = 'л';}
    elsif ($ran_num < 2359) {$prefix = 'он';}
    elsif ($ran_num < 2364) {$prefix = 'ав';}
    elsif ($ran_num < 2365) {$prefix = 'ял';}
    elsif ($ran_num < 2369) {$prefix = 'ящ';}
    elsif ($ran_num < 2370) {$prefix = 'эс';}
    elsif ($ran_num < 2391) {$prefix = 'им';}
    elsif ($ran_num < 2392) {$prefix = 'иер';}
    elsif ($ran_num < 2484) {$prefix = 'эт';}
    elsif ($ran_num < 2489) {$prefix = 'ид';}
    elsif ($ran_num < 2491) {$prefix = 'ур';}
    elsif ($ran_num < 2493) {$prefix = 'эв';}
    elsif ($ran_num < 2495) {$prefix = 'уп';}
    elsif ($ran_num < 2507) {$prefix = 'оч';}
    elsif ($ran_num < 2603) {$prefix = 'г';}
    elsif ($ran_num < 2604) {$prefix = 'эр';}
    elsif ($ran_num < 2619) {$prefix = 'ин';}
    elsif ($ran_num < 2644) {$prefix = 'х';}
    elsif ($ran_num < 2661) {$prefix = 'ис';}
    elsif ($ran_num < 2666) {$prefix = 'ух';}
    elsif ($ran_num < 2667) {$prefix = 'уй';}
    elsif ($ran_num < 2668) {$prefix = 'ас';}
    elsif ($ran_num < 2693) {$prefix = 'ш';}
    elsif ($ran_num < 2707) {$prefix = 'уж';}
    elsif ($ran_num < 2750) {$prefix = 'ег';}
    elsif ($ran_num < 2752) {$prefix = 'яр';}
    elsif ($ran_num < 2753) {$prefix = 'юг';}
    elsif ($ran_num < 2757) {$prefix = 'ор';}
    elsif ($ran_num < 2974) {$prefix = 'д';}
    elsif ($ran_num < 2977) {$prefix = 'аз';}
    elsif ($ran_num < 2980) {$prefix = 'юн';}
    elsif ($ran_num < 2982) {$prefix = 'ауд';}
    elsif ($ran_num < 2985) {$prefix = 'яс';}
    elsif ($ran_num < 2988) {$prefix = 'аг';}
    elsif ($ran_num < 3477) {$prefix = 'в';}
    elsif ($ran_num < 3480) {$prefix = 'ел';}
    elsif ($ran_num < 3482) {$prefix = 'эл';}
    elsif ($ran_num < 3483) {$prefix = 'эм';}
    elsif ($ran_num < 3490) {$prefix = 'ар';}
    elsif ($ran_num < 3491) {$prefix = 'ал';}
    elsif ($ran_num < 3502) {$prefix = 'ус';}
    elsif ($ran_num < 3504) {$prefix = 'уб';}
    elsif ($ran_num < 3505) {$prefix = 'уяс';}
    elsif ($ran_num < 3506) {$prefix = 'ез';}
    elsif ($ran_num < 3507) {$prefix = 'эк';}
    elsif ($ran_num < 3514) {$prefix = 'оп';}
    elsif ($ran_num < 4002) {$prefix = 'н';}
    elsif ($ran_num < 4003) {$prefix = 'еж';}
    elsif ($ran_num < 4004) {$prefix = 'оц';}
    elsif ($ran_num < 4010) {$prefix = 'уз';}
    elsif ($ran_num < 4017) {$prefix = 'аб';}
    elsif ($ran_num < 4174) {$prefix = 'м';}
    elsif ($ran_num < 4311) {$prefix = 'з';}
    elsif ($ran_num < 4349) {$prefix = 'ж';}
    elsif ($ran_num < 4350) {$prefix = 'ож';}
    elsif ($ran_num < 4369) {$prefix = 'ес';}
    elsif ($ran_num < 4372) {$prefix = 'уг';}
    elsif ($ran_num < 4378) {$prefix = 'ув';}
    elsif ($ran_num < 4400) {$prefix = 'од';}
    elsif ($ran_num < 4928) {$prefix = 'с';}
    elsif ($ran_num < 4929) {$prefix = 'оз';}
    elsif ($ran_num < 4934) {$prefix = 'ак';}
    elsif ($ran_num < 4938) {$prefix = 'оф';}
    elsif ($ran_num < 4944) {$prefix = 'ещ';}
    elsif ($ran_num < 4954) {$prefix = 'ем';}
    elsif ($ran_num < 4958) {$prefix = 'яз';}
    elsif ($ran_num < 4975) {$prefix = 'ок';}

    return $prefix;
